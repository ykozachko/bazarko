<p>
  <%= link_to "Back", categories_path%>
Category Details
</p>

<h2><%= @category.name %></h2>

<p><%= link_to "Edit", edit_category_url(@category)%></p>

<%= link_to "Delete", category_url(@category), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %></p>


<hr>

<h2>Items</h2>

<%# @category.items.each do |item| %>
  <%#= render 'items/item', item: item %>
<%# end %>

<%= render @category.items %>