<div class="card-header">
  <div>
    <p><%= link_to "Back", admin_category_path(@category) %></p>
    <h2 class="cme-40">Edit Category</h2>
  </div>
</div>

<div class="card-body">
  <%= render 'shared/errors', resource: @category %>
  <%= render 'form', category: @category %>
</div>